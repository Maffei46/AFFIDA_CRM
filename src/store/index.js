import { createStore } from 'vuex'

import state from './models/state'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    state
  }
})
